# 工作报告：图文使用说明（2026-09-24）

## 一、完成的工作

- 新增面向最终用户的**图文使用说明** `docs/USER-GUIDE.md`：下载安装（含本版新增的安装目录/数据目录选择）、启动终端状态逐行解读、桌面界面导览、8 个官方工具的安装与登录命令表、Work 第一个任务全流程、Teams 三步创建表单与三种策略、手动终端入口、常见问题排查表。全部配 `docs/images/` 既有截图。
- **与实现逐项核对**后才定稿：导航项、按钮文字、表单字段名均直接取自源码字符串，避免图文说明与实际界面漂移——
  - 顶部导航实为 **Work / Chat / Teams / 应用 / 项目 / API 对话**（`src/bin/desktop/ui.rs:66`）；
  - 应用页按钮是**「检测安装」**（非"探测"）、**「打开终端」**、**「诊断详情」**（`studio.rs:1279-1310`）；
  - Work 表单字段：任务名称/任务目标与范围/执行应用/模型 ID/推理档位/项目/只读工作区/最长运行/验收标准，按钮**「创建并开始 →」「保存草稿」**，草稿启动按钮是**「开始任务」**（`studio.rs:617-678`）；不存在"验收通过"按钮，任务侧终态操作为 取消/复制为新草稿/复制结果；
  - Teams 创建按钮**「创建协作计划 →」**，三步表单 01 定义目标 / 02 指定执行器（固定官方执行器、逐节点指定模型、Automatic·条件检查）/ 03 验证完成（程序 + JSON 参数 + 超时）（`teams.rs:559-625`）；Automatic 当前界面如实提示「尚未就绪」；
  - 启动器示例输出与 `packaging/windows/Start-Wonderland.ps1` 逐行一致，失败红字 `[错误]`、15 秒停留、日志路径 `wonderland.err.log / wonderland.out.log`、端口环境变量名 `AGENT_ADDR` / `AGENT_SERVER_URL` 全部核对。
- 指南登记进两个文档入口：`docs/README.md`（文档入口首条）与 `docs/DOCUMENT-GUIDE-2026-09-23.md`（第一节入口表）。

## 二、各文件的说明

| 文件 | 变更 |
| --- | --- |
| `docs/USER-GUIDE.md` | 新增，本报告主体交付物；引用 `images/` 下 8 张截图中的 6 张（welcome、work-new、work-board、teams、teams-new、terminal） |
| `docs/README.md` | 「文档入口」首行加入 USER-GUIDE 一句话简介 |
| `docs/DOCUMENT-GUIDE-2026-09-23.md` | 第一节入口表加入 USER-GUIDE 行，标明"新用户上手读这份" |

未改任何源码、安装器与启动器。

## 三、需要做的测试

- 人工通读一遍指南，对照 0.11.1 安装包实际界面核对每一处按钮文字（尤其后续版本若改 UI 文案，需同步本指南）。
- 截图与版本一致性：6 张截图均摄自 0.11.1 桌面端；若后续换 UI 皮肤需重摄。
- GitHub 渲染检查：推送后打开仓库页确认相对图片链接 `images/*.png` 正常显示。

## 四、还未完成的任务与完成思路

- **指南尚未覆盖 CLI**（wonderland-cli / npm CLI）：面向的是"不太会用"的图形界面用户，刻意不含命令行用法；需要时在指南追加第九节，按 `DEVELOPER_HANDOFF.md` 的双 CLI 语法差异表改写成用户口吻。
- **截图缺口**：应用页（检测安装卡片）、Teams 详情路由决策卡片目前无专属截图，指南相应位置用文字描述兜底。后续在真实账号验证时补摄两张，插入第 4、6 节即可。
- **真实账号验证**（用户亲自执行）：按 [PROJECT-TASK-REPORT](PROJECT-TASK-REPORT-2026-09-23.md) 第三节清单——安装 MiMo/Kimi Code/MiniMax/Grok 1.0.38、真实登录、真实推理、跨厂商 Automatic 团队测试；验证中发现的问题回流修订本指南。
