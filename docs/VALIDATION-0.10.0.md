# 0.10.0 验证记录

日期：2026-09-21，Windows x64。该版本扩展官方工具适配与安装诊断，未进行跨厂商效率对照，也未证明同等质量或成本。

## 回归

完整 `cargo test --locked --all-targets --features ui-snapshots`：397 项库测试、3 项 Rust CLI 测试、17 项桌面测试、7 项协议集成测试通过，7 项外部条件测试默认忽略。随后复核补充 Claude 未完成消息的终止校验，并单独运行该适配器全部 13 项测试，通过。合计 425 个不同的 Rust 测试通过；CI 对最终提交重新运行完整回归。

npm CLI 的 14 项测试通过，包括安装诊断路由、服务错误传播，以及拒绝单任务中尚未支持的显式推理参数。`cargo fmt --all -- --check` 通过。协议测试覆盖精确模型/推理绑定、跨 provider 拒绝、重复/过期权限、工具生命周期、块级流式去重、未完成消息拒绝成功、取消与有界清理。

## 真实官方程序检查

- Claude Code 官方原生 npm 版 2.1.193：离线初始化、有效设置检查通过；官方 CLI 连接本地合成 Anthropic SSE 服务，完成思考/正文/结果协议循环。轨迹已脱敏保存为回归 fixture。安装诊断成功返回版本和入口指纹。没有调用真实 Claude 模型。
- DeepSeek 官方 `@deepseek-ai/dsh@0.1.6-alpha.2`：隔离 npm 安装的 249 个 DSH 系列包版本一致；真实 CLI 完成 ACP initialize、创建会话、精确模型/推理选项设置和关闭。显式运行忽略的官方集成测试通过。没有传入 API 密钥或发送推理提示。Windows Node 路径规范化经过真实程序验证。
- 0.9.0 使用已授权 Kimi 订阅完成的独立项目协作及 12 项验收，保留于 [0.9.0 验证记录](VALIDATION-0.9.0.md)。本版新增协议验证不替代实际模型推理验证。

DeepSeek 当前的安装诊断以 PATH 为准；隔离执行器安装可通过 `WONDERLAND_DSH_CLI` 使用，不能把这项测试安装视为全局安装。只读边界依赖官方工具策略，握手不证明实际工具沙箱隔离。

## 桌面检查

应用页在 1280×820 和 940×620 渲染并检查。展示受管能力、检测安装、创建任务、终端入口和官方文档；Claude 示例诊断展示版本、入口及认证未知说明。页面使用滚动区域容纳窄窗口内容。截图明确标注“界面展示样例 · 非真实执行结果”，不作为模型执行证据。

## 发布构建与 API 检查

`cargo build --locked --release --bins` 通过。隔离启动的 release 后端返回四个受管执行器；Rust 和 npm CLI 均通过新版 API 完成 Claude 安装诊断。Claude、DeepSeek 只读草稿创建成功并保持 draft，随后取消，未调用模型；跨 provider 模型绑定在创建时拒绝。

0.10.0 后端重新打开 0.9.0 的独立测试数据目录，真实 Kimi Team 仍为 succeeded，12 项验收输出和隔离集成目录可通过 Rust CLI 读取。数据库和账号配置不作为发布文件。

## 尚未验证

真实 Claude/DeepSeek 付费推理、双厂商协作完成同一任务、原生恢复/分叉、订阅额度读取和模型效率对照尚未验证。自动成本/质量选模、预算硬限制及更多产品面板仍在开发，不属于本次验证通过范围。
